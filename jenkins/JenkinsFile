node('jenkins-slave') {
    
     stage('test pipeline') {
        sh(script: """
            echo "hello"
           git clone https://github.com/thevictorgreen/jnlp-test.git
           cd ./jnlp-test/golang
           
           docker build . -t test
        """)
    }
}
